{
  "name": "ahashan-portfolio",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "portfolio"
  ],
  "scripts": {
    "dev": "PORT=3000 npm run --workspace portfolio dev",
    "build": "npm run --workspace portfolio build",
    "typecheck": "npm run --workspace portfolio typecheck",
    "serve": "npm run --workspace portfolio serve"
  },
  "devDependencies": {
    "typescript": "~5.9.2"
  }
}
